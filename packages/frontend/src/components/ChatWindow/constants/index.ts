/**
 * ChatWindow constants
 */

/** Distance from bottom (in pixels) to consider "at bottom" */
export const SCROLL_BOTTOM_THRESHOLD = 100;

/** Debounce delay for scroll event handling (in milliseconds) */
export const SCROLL_DEBOUNCE_DELAY = 150;

/** Delay before auto-scrolling on task switch (in milliseconds) */
export const TASK_SWITCH_SCROLL_DELAY = 100;
